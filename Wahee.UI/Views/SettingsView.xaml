<UserControl x:Class="Wahee.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600"
             FlowDirection="RightToLeft">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="600" HorizontalAlignment="Right" Margin="10">

            <Border Style="{StaticResource ModernCardStyle}" Padding="25" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="الإشعارات" FontSize="18" Foreground="{DynamicResource TextForegroundBrush}" FontWeight="SemiBold" Margin="0,0,0,20"/>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="تنبيهات الأذان" Foreground="{DynamicResource TextForegroundBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="إشعار صوتي عند دخول وقت الصلاة" Foreground="#88FFFFFF" FontSize="12"/>
                        </StackPanel>
                        <ToggleButton Grid.Column="1" Style="{StaticResource ModernToggleStyle}" IsChecked="{Binding AdhanEnabled, Mode=TwoWay}"/>
                    </Grid>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="تذكير الأذكار" Foreground="{DynamicResource TextForegroundBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="تذكير بأذكار الصباح والمساء" Foreground="#88FFFFFF" FontSize="12"/>
                        </StackPanel>
                        <ToggleButton Grid.Column="1" Style="{StaticResource ModernToggleStyle}" IsChecked="{Binding AzkarReminder, Mode=TwoWay}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource ModernCardStyle}" Padding="25" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="عام" FontSize="18" Foreground="{DynamicResource TextForegroundBrush}" FontWeight="SemiBold" Margin="0,0,0,20"/>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="بدء التشغيل مع ويندوز" Foreground="{DynamicResource TextForegroundBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="تشغيل البرنامج تلقائياً عند بدء ويندوز" Foreground="#88FFFFFF" FontSize="12"/>
                        </StackPanel>
                        <ToggleButton Grid.Column="1" Style="{StaticResource ModernToggleStyle}" IsChecked="{Binding StartWithWindows, Mode=TwoWay}"/>
                    </Grid>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="التصغير إلى System Tray" Foreground="{DynamicResource TextForegroundBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="استمرار عمل البرنامج في الخلفية" Foreground="#88FFFFFF" FontSize="12"/>
                        </StackPanel>
                        <ToggleButton Grid.Column="1" Style="{StaticResource ModernToggleStyle}" IsChecked="{Binding MinimizeToTray, Mode=TwoWay}"/>
                    </Grid>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="تحديد الموقع تلقائياً" Foreground="{DynamicResource TextForegroundBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="استخدام IP لتحديد مواقيت الصلاة" Foreground="#88FFFFFF" FontSize="12"/>
                        </StackPanel>
                        <ToggleButton Grid.Column="1" Style="{StaticResource ModernToggleStyle}" IsChecked="{Binding AutoDetectLocation, Mode=TwoWay}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource ModernCardStyle}" Padding="25" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="الموقع والتوقيت" FontSize="18" Foreground="{DynamicResource TextForegroundBrush}" FontWeight="SemiBold" Margin="0,0,0,20"/>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="المدينة" Foreground="{DynamicResource TextForegroundBrush}" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Width="220" IsEnabled="{Binding IsManualLocationEnabled}" Text="{Binding ManualCity, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="الدولة" Foreground="{DynamicResource TextForegroundBrush}" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Width="220" IsEnabled="{Binding IsManualLocationEnabled}" Text="{Binding ManualCountry, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="وضع التوقيت" Foreground="{DynamicResource TextForegroundBrush}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" Width="220" SelectedValuePath="Tag" SelectedValue="{Binding SeasonMode, Mode=TwoWay}">
                            <ComboBoxItem Content="تلقائي" Tag="Auto"/>
                            <ComboBoxItem Content="توقيت شتوي" Tag="Winter"/>
                            <ComboBoxItem Content="توقيت صيفي (+1 ساعة)" Tag="Summer"/>
                        </ComboBox>
                    </Grid>

                    <TextBlock Text="عند اختيار التوقيت الصيفي سيتم إضافة ساعة واحدة للمواقيت المعروضة." Foreground="#88FFFFFF" FontSize="12"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource ModernCardStyle}" Padding="25" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="المظهر" FontSize="18" Foreground="{DynamicResource TextForegroundBrush}" FontWeight="SemiBold" Margin="0,0,0,20"/>
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="الثيم" Foreground="{DynamicResource TextForegroundBrush}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1"
                                  Width="180"
                                  Background="#22FFFFFF"
                                  Foreground="{DynamicResource TextForegroundBrush}"
                                  BorderThickness="0"
                                  Padding="10,8"
                                  SelectedItem="{Binding SelectedTheme, Mode=TwoWay}">
                            <ComboBoxItem Content="Dark"/>
                            <ComboBoxItem Content="Light"/>
                        </ComboBox>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource ModernCardStyle}" Padding="25" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="عن البرنامج" FontSize="18" Foreground="{DynamicResource TextForegroundBrush}" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <TextBlock Text="الإصدار: 1.0.6" Foreground="{DynamicResource TextForegroundBrush}" Margin="0,0,0,8"/>
                    <TextBlock Text="المطور: Mohamed Gamal" Foreground="{DynamicResource TextForegroundBrush}" Margin="0,0,0,8"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
