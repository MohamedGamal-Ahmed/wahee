<UserControl x:Class="Wahee.UI.Views.AzkarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="700"
             FlowDirection="RightToLeft">

    <!-- Task Summary:
         - Phase 2 / Task 4: Bound Azkar view state/commands to AzkarViewModel. -->
    <UserControl.Resources>
        <Style x:Key="AzkarTabStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#88FFFFFF"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="30,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="#0AFFFFFF" CornerRadius="16" Padding="10" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Style="{StaticResource AzkarTabStyle}" Content="☀️ تحميل الأذكار" Command="{Binding LoadAzkarCommand}"/>
                <Button Style="{StaticResource AzkarTabStyle}" Content="🔄 ذكر جديد" Margin="10,0,0,0" Command="{Binding NextAzkarCommand}"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource ModernCardStyle}">
            <Grid>
                <ProgressBar IsIndeterminate="True"
                             VerticalAlignment="Top"
                             Height="3"
                             Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

                <Grid Margin="30">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel HorizontalAlignment="Center" Margin="0,0,0,20">
                        <TextBlock Text="{Binding CurrentCount, StringFormat=العدد: {0}}" FontSize="18" Foreground="{DynamicResource AccentHighlightBrush}" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding CurrentAzkar.Source}" FontSize="12" Foreground="#66FFFFFF" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <Button Background="Transparent" BorderThickness="0" Cursor="Hand" Command="{Binding CountCommand}">
                            <Border Background="#0AFFFFFF" CornerRadius="16" Padding="30">
                                <TextBlock Text="{Binding CurrentAzkar.Content}"
                                           TextWrapping="Wrap"
                                           FontSize="24"
                                           Foreground="{DynamicResource TextForegroundBrush}"
                                           TextAlignment="Center"
                                           LineHeight="48"
                                           FontFamily="Traditional Arabic"/>
                            </Border>
                        </Button>
                    </ScrollViewer>

                    <TextBlock Grid.Row="2" Text="اضغط على الذكر للتسبيح" FontSize="12" Foreground="#44FFFFFF" HorizontalAlignment="Center" Margin="0,15,0,0"/>
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="#0AFFFFFF" CornerRadius="16" Padding="15" Margin="0,15,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Style="{StaticResource ModernButtonStyle}" Background="#22FFFFFF" Padding="20,10" Command="{Binding LoadAzkarCommand}">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M15.41,16.58L10.83,12L15.41,7.41L14,6L8,12L14,18L15.41,16.58Z" Fill="White" Stretch="Uniform" Width="16" Height="16"/>
                        <TextBlock Text="إعادة تحميل" Foreground="{DynamicResource TextForegroundBrush}" Margin="8,0,0,0"/>
                    </StackPanel>
                </Button>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0">
                    <ProgressBar Height="6" Minimum="0" Maximum="100" Value="50" Background="#22FFFFFF" Foreground="{DynamicResource AccentBrush}"/>
                    <TextBlock Text="{Binding AzkarList.Count, StringFormat=عدد الأذكار: {0}}" FontSize="12" Foreground="#88FFFFFF" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>

                <Button Grid.Column="2" Style="{StaticResource ModernButtonStyle}" Background="{DynamicResource AccentBrush}" Padding="20,10" Command="{Binding NextAzkarCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="التالي" Foreground="{DynamicResource TextForegroundBrush}" Margin="0,0,8,0"/>
                        <Path Data="M8.59,16.58L13.17,12L8.59,7.41L10,6L16,12L10,18L8.59,16.58Z" Fill="White" Stretch="Uniform" Width="16" Height="16"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
